name: Deploy Website

on:
  push:
    branches:
      - main
  issues:
    types:
      - opened
      - edited
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "8.x"

      - name: Install dotnet-script
        run: dotnet tool install -g dotnet-script

      - name: Install Dependencies
        run: |
          dotnet script add package Newtonsoft.Json
          dotnet script add package HtmlAgilityPack

      - name: Generate Site
        run: dotnet script fetch_issues.cs

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./posts
